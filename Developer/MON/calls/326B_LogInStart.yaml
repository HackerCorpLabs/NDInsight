# SINTRAN III Monitor Call: LogInStart
# Octal: 326B
# Extracted from: Monitor Calls.md (ND-860228.2 EN)

octal: 326B
name: LogInStart
short_names:
- MLOGI
description: Logs in a user on a terminal and starts a subsystem.
notes:
- The terminal must be free.
- The subsystem must be reentrant. See @DUMP-PROGRAM-REENTRANT.
see_also:
- SetUserParam
- '@DUMP-PROGRAM-REENTRANT'
parameters:
- name: TermNo
  type: INTEGER
  io: I
  description: Logical device number of a terminal.
- name: UserName
  type: STRING
  io: I
  description: ''
- name: Password
  type: STRING
  io: I
  description: ''
- name: ProjPassword
  type: STRING
  io: I
  description: Project password.
- name: Subsystem
  type: STRING
  io: I
  description: ''
- name: UserParam
  type: ARRAY
  io: I
  description: The 5 user parameters. See SetUserParam. Array of 5 elements.
- name: Status
  type: INTEGER
  io: O
  description: Return status. Unsuccessful log in returns -1.
examples:
  pascal:
    available: true
    code: |-
      TermNo : INTEGER2;
      UserName : PACKED ARRAY [0..63] OF CHAR;
      Password, ProjPassword : PACKED ARRAY [0..15] OF CHAR;
      Subsystem : PACKED ARRAY [0..31] OF CHAR;
      UserParam : RECORD END;
      ...
      LogInStart(TermNo, UserName, Password, ProjPassword, Subsystem, UserParam);
      IF ErrCode <> 0 THEN ...
  cobol:
    available: true
    code: |-
      01 TermNo COMP.
      01 UserName PIC X(64).
      01 Password PIC X(16).
      01 ProjPassword PIC X(16).
      01 Subsystem PIC X(32).
      01 UserParam.
      02 array COMP OCCURS 5 TIMES.
      01 ErrCode COMP.
      ...
      MONITOR-CALL "LogInStart" USING TermNo, UserName, Password, ProjPassword, Subsystem, UserParam.
      CALL "CbError" USING ErrCode.
      IF ErrCode NOT = 0 GO ...
  fortran:
    available: true
    code: |-
      INTEGER TermNo
      CHARACTER UserName*64, Password*16, ProjPassword*16, Subsystem*32
      INTEGER UserParam(5)
      ...
      Monitor_Call('LogInStart', TermNo, UserName(1:64), Password(1:16), ProjPassword(1:16), Subsystem(1:32), UserParam(1))
      IF (ErrCode .NE. 0) THEN ...
  planc:
    available: true
    code: |-
      INTEGER : TermNo
      BYTES : UserName(0:63), Password(0:15), ProjPassword(0:15), Subsystem(0:31)
      INTEGER ARRAY UserParam(0:4)
      ...
      ON ROUTINEERROR DO
      IF ErrCode <> 0 THEN ...
      ENDON
      Monitor_Call('LogInStart', TermNo, UserName, Password, ProjPassword, &
          Subsystem, UserParam(0))
  assembly_500:
    available: false
    code: ''
    mon_number: null
    notes: Not available.
  mac:
    available: true
    code: |-
      LDA (PAR                 %Load register A with address of parameter list.
      MON 326                 %Monitor call LogInStart.
      JMP ERROR               %Handle error if STAT is negative.
      ...
      PAR, TERNO,             %Terminal number
      USER,                   %User name
      PASSW,                  %Password.
      PROJP,                  %Project password.
      SUBSYS,                 %Subsystem
      USRPAR,                 %User parameter.
      STAT,                   %Status returned.
      ...
      USER, 'A-HANSEN'        %Login user A-HANSEN.
      PASSW, 'MAY'            %Use MAY as password.
      PROJP, 'CHEESE'         %Use CHEESE as a project password.
      SUBSYS, 'NOTIS-WP'      %Use NOTIS-WP as a subsystem.
      STAT, 0                 %
      ERROR, ...              %Error number in register A.
    mon_number: 326
    notes: ''
compatibility:
  nd100: true
  nd500: false
  user_programs: true
  rt_programs: true
  system_programs: true
source:
  document: SINTRAN III Monitor Calls (ND-860228.2 EN)
  page: 0
  line: 15227
  scan_date: '2020'
  scanned_by: Jonny Oddene for Sintran Data
extraction:
  date: '2025-01-06'
  version: '1.0'
  validated: true
  ocr_corrected: false
