# SINTRAN III Monitor Call: ClearInBuffer
# Octal: 13B
# Extracted from: Monitor Calls.md (ND-860228.2 EN)

octal: 13B
name: ClearInBuffer
short_names:
- CIBUF
description: |-
  Clears a device input buffer. Input from character devices, e.g. terminals, are temporarily stored in this buffer.

  - You can use logical device number 1 for your own terminal in background programs.
see_also:
- '@CLEAR-DEVICE'
- ClearOutBuffer
- and DeviceControl
parameters:
- name: DeviceNumber
  type: INTEGER
  io: I
  description: Logical device number. See appendix B.
examples:
  pascal:
    available: true
    code: |-
      DeviceNumber : INTEGER2;
      ...
      ClearInBuffer(DeviceNumber);
      IF ErrCode <> 0 THEN ...
  cobol:
    available: true
    code: |-
      01 DeviceNumber COMP.
      01 ErrCode COMP.
      ...
      MONITOR-CALL "ClearInBuffer" USING DeviceNumber.
      CALL "CbError" USING ErrCode.
      IF ErrCode NOT = 0 GO ...
  fortran:
    available: true
    code: |-
      INTEGER DeviceNumber
      ...
      Monitor_Call('ClearInBuffer', DeviceNumber)
      IF (ErrCode .NE. 0) THEN ...
  planc:
    available: true
    code: |-
      INTEGER : DeviceNumber
      ...
      ON ROUTINEERROR DO
          IF ErrCode >< 0 THEN ...
      ENDON
      Monitor_Call('ClearInBuffer', DeviceNumber)
  assembly_500:
    available: true
    code: |-
      DeviceNumber : W BLOCK 1
      ErrCode : W BLOCK 1
      ClearInBuffer : EQU 37B9 + 13B
      ...
      CALLG ClearInBuffer, 1, DeviceNumber
      IF K GO ERROR
      ...
      ERROR : W1 =: ErrCode            %ErrorCode in W1 register.
    octal_definition: 37B9 + 13B
  mac:
    available: true
    code: |-
      LDT   DEVNO       %Load register T with logical device number.
      MON   13          %Monitor call ClearInBuffer.
      JMP   ERROR       %Error return from monitor call.
      ...               %Normal return.
      ERROR, ...        %Error number in register A.
      ...
      DEVNO, ...


      | ND-100 and ND-500 | All users | All programs |
    mon_number: 13
compatibility:
  nd100: true
  nd500: true
  user_programs: true
  rt_programs: true
  system_programs: false
source:
  document: SINTRAN III Monitor Calls (ND-860228.2 EN)
  page: 113
  line: 4672
  scan_date: '2020'
  scanned_by: Jonny Oddene for Sintran Data
extraction:
  date: '2025-01-06'
  version: '1.0'
  validated: false
  ocr_corrected: false
emulation:
  status: stub
  nd500x_handler: /home/ronny/repos/nd500x/src/libmon/handlers/mon_13B_ClearInBuffer.c
  last_updated: '2026-07-17'

  stub_behaviour: >-
    SUCCESS NO-OP - SAY SO. 13B CIBUF returns success WITHOUT DISCARDING ANYTHING.
    Two reasons, both deliberate: (1) the emulator has no separate hardware
    type-ahead buffer to flush; (2) the queued-console input models the program's
    OWN SCRIPTED command stream, and clearing it would drop commands the program
    still intends to read. A guest that relies on CIBUF actually discarding
    type-ahead will NOT behave correctly here.

  observed_calls:
    - caller: 'ND linker (linker-b01.dom), startup'
      params:
        DeviceNumber: 'not recorded'
      expectation: >-
        Called during linker startup terminal configuration. The linker only needs
        it not to fail; the success no-op is enough for it to proceed.
      note: >-
        Before this handler existed the auto-generated stub returned error -1 and
        the linker crashed in startup device configuration. Commit 940ba39.

  return_contract:
    success: 'K flag cleared.'
    errors:
      - code: 111
        octal: 157B
        meaning: 'Missing parameter (no CALLG args)'

  verified:
    - claim: 'The linker proceeds past its startup with the success no-op.'
      evidence: >-
        Commit 940ba39: "13B CIBUF (ClearInBuffer): success no-op". The linker then
        advanced to MON 144B MAGTP. ctest 16/16 green, 58/58 MON unit tests.

  unverified:
    - >-
      THE CARVE WAS NOT MINED FOR THIS ENTRY. A carve folder exists at
      /mnt/e/Dev/Ronny/NDInsight/tools/sintran-segment-carver/versions/L-VSX-500/re/mon-analysis/013B-ClearInBuffer/
      and has NOT been read into this block. The real worker's semantics (which
      buffer, what it does to a pending break count, what it returns for a
      non-terminal device) are therefore unknown here.
    - >-
      nd500x ignores DeviceNumber entirely - it does not validate the device class
      and does not reject file numbers. Whether real CIBUF errors on a file number
      is unknown.

  sources:
    - doc: 'SINTRAN III Monitor Calls (ND-860228.2 EN)'
      note: 'The manual body of this YAML.'
