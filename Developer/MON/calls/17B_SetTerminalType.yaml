# SINTRAN III Monitor Call: SetTerminalType
# Octal: 17B
# Extracted from: Monitor Calls.md (ND-860228.2 EN)

octal: 17B
name: SetTerminalType
short_names:
- MSTTY
description: ''
see_also:
- GetTerminalType
- '@SET-TERMINAL-TYPE'
parameters:
- name: Param1
  type: UNKNOWN
  io: I
  description: The logical device number of the terminal. You may use 1 for your own terminal in background
    programs.
- name: Param2
  type: UNKNOWN
  io: I
  description: The terminal type.
- name: Param3
  type: UNKNOWN
  io: I
  description: Standard Error Code. See appendix A.
examples:
  pascal:
    available: true
    code: |-
      DeviceNumber, TerminalType : INTEGER2;
      ...
      SetTerminalType(DeviceNumber, TerminalType);
      IF ErrCode <> 0 THEN ...
  cobol:
    available: true
    code: |-
      01 DeviceNumber COMP.
      01 TerminalType COMP.
      01 ErrCode COMP.
      ...
      MONITOR-CALL "SetTerminalType" USING DeviceNumber, TerminalType.
      CALL "CbError" USING ErrCode.
      IF ErrCode NOT = 0 GO ...
  fortran:
    available: true
    code: |-
      INTEGER DeviceNumber, TerminalType
      ...
      Monitor Call('SetTerminalType', DeviceNumber, TerminalType)
      IF (ErrCode .NE. 0) THEN ...
  planc:
    available: true
    code: |-
      INTEGER : DeviceNumber, TerminalType
      ...
      ON ROUTINEERROR DO
          IF ErrCode <> 0 THEN ...
      ENDON

      Monitor_Call('SetTerminalType', DeviceNumber, TerminalType)
  assembly_500:
    available: true
    code: |-
      DeviceNumber : W BLOCK 1
      TerminalType : W BLOCK 1
      ErrCode : W BLOCK 1
      SetTerminalType : EQU 37B9 + 17B
      ...
      CALLG SetTerminalType, 2, DeviceNumber, TerminalType
      IF K GO ERROR
      ...

      ERROR : W1 =: ErrCode             %ErrorCode in W1 register.
    octal_definition: 37B9 + 17B
  mac:
    available: true
    code: |-
      LDT DEVNO        %Logical device number.
      LDA TYPE         %Terminal type number.
      MON 17           %Monitor call SetTerminalType.
      JMP ERROR        %Error return from monitor call.
      ...              %Normal return.
      ERROR, ...       %Error number in register A.
      ...

      DEVNO, ...
      TYPE, ...


      | ND-100 and ND-500 | All users | All programs |
    mon_number: 17
compatibility:
  nd100: true
  nd500: true
  user_programs: true
  rt_programs: true
  system_programs: false
source:
  document: SINTRAN III Monitor Calls (ND-860228.2 EN)
  page: 477
  line: 21767
  scan_date: '2020'
  scanned_by: Jonny Oddene for Sintran Data
extraction:
  date: '2025-01-06'
  version: '1.0'
  validated: false
  ocr_corrected: false
