# SINTRAN III Monitor Call: OutBufferSpace
# Octal: 67B
# Extracted from: Monitor Calls.md (ND-860228.2 EN)

octal: 67B
name: OutBufferSpace
short_names:
- OSIZE
description: ''
see_also:
- ClearOutBuffer
- InBufferSpace
parameters:
- name: Param1
  type: UNKNOWN
  io: I
  description: Logical device number. See appendix B.
- name: Param2
  type: UNKNOWN
  io: I
  description: Number of bytes which can be written before the program must wait.
- name: Param3
  type: UNKNOWN
  io: I
  description: Standard Error Code. See appendix A.
examples:
  pascal:
    available: true
    code: |-
      DeviceNumber, NoOfBytes : INTEGER2;
      ...
      OutBufferSpace(DeviceNumber, NoOfBytes);
      IF ErrCode <> 0 THEN ...
  cobol:
    available: true
    code: |-
      01 DeviceNumber  COMP.
      01 NoOfBytes     COMP.
      01 ErrCode       COMP.
      ...
      MONITOR-CALL "OutBufferSpace" USING DeviceNumber, NoOfBytes.
      CALL "CbError" USING ErrCode.
      IF ErrCode NOT = 0 GO ...
  fortran:
    available: true
    code: |-
      INTEGER DeviceNumber, NoOfBytes
      ...
      Monitor_Cal1('OutBufferSpace', DeviceNumber, NoOfBytes)
      IF (ErrCode .NE. 0) THEN ...
  planc:
    available: true
    code: |-
      INTEGER : DeviceNumber, NoOfBytes
      ...
      ON ROUTINEERROR DO
          IF ErrCode > 0 THEN ...
      ENDON
      Monitor_Call('OutBufferSpace', DeviceNumber, NoOfBytes)
  assembly_500:
    available: true
    code: |-
      DeviceNumber : W BLOCK 1
      NoOfBytes : W BLOCK 1
      ErrCode : W BLOCK 1
      OutBufferSpace : EQU 37B9 + 67B
          ...
          CALLG OutBufferSpace, 1, DeviceNumber
          IF K GO ERROR
          W1 =: NoOfBytes           %Result is returned in W1 register.
          ...
      ERROR : W1 =: ErrCode        %ErrorCode in W1 register.
    octal_definition: 37B9 + 67B
  mac:
    available: true
    code: |2-
          LDT DEVNO                %Logical device number.
          MON 67                   %Monitor call OutBufferSpace.
          JMP ERROR                %Error return from monitor call.
          STA COUNT                %Normal return, store number of bytes.
      ERROR,
          ...                      %Error number in register A.
      DEVNO, ...
      COUNT, 0                     %Number of bytes free space in outbuffer.


      | ND-100 and ND-500 | All users | All programs |
      |-------------------|-----------|--------------|
    mon_number: 67
compatibility:
  nd100: true
  nd500: true
  user_programs: false
  rt_programs: false
  system_programs: false
source:
  document: SINTRAN III Monitor Calls (ND-860228.2 EN)
  page: 375
  line: 16947
  scan_date: '2020'
  scanned_by: Jonny Oddene for Sintran Data
extraction:
  date: '2025-01-06'
  version: '1.0'
  validated: false
  ocr_corrected: false
