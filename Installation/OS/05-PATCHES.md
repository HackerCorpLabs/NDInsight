# 05 — Applying Patches

> Status: SCAFFOLD.

Verified artifacts:
- On disk: `PATCHES:OUT`, `PATCH-FILE:OUT` (`BIGDISK0-K.txt`).
- SINTRAN-K archive `FILE-INFO/ND-PATCH-SIN-K.txt`; SINTRAN-M archive `FILE-INFO/ND-PATCH-SIN-M.TXT`.
- SINTRAN-M archive `Admin/PATCH-LOG.txt`.

## TODO
- The patch application procedure (tool, commands, order relative to S3 and startup).
- Contents/format of the patch files (transcribe from source, no inference).
- Per-version patch sets.
