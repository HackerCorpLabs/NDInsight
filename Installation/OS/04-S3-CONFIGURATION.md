# 04 — S3 Configuration (System Generation)

> Status: SCAFFOLD.

Verified: the golden disk contains `S3-CONFIGURATION:PROG` and `S3-CONFIGURATION:CNFG`
(`BIGDISK0-K.txt`). S3 is the SINTRAN III configuration/system-generation tool.

## TODO
- What S3 configures (devices, sizes, options) and when it runs in the install order.
- `:PROG` (the tool) vs `:CNFG` (the configuration data) roles.
- Per-version differences.
- Source: locate S3 documentation (likely in System Supervisor / System Documentation manuals).
