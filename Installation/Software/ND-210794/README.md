# ND-210794 — NOTIS-DS for ND-500

> Status: STUB — both real floppy images mount but are completely empty (0 files); confirms the imager's own "errors" report

| Field | Value |
|-------|-------|
| ND article number | `ND-210794` |
| Product name | NOTIS-DS for ND-500 |
| Functional category | Office — NOTIS suite |
| CPU target | ND-500 |

## What is known

Two floppy images, `210794B00-EN-01D`/`-02D` (downloaded via NDwiki, imaged by Torfinn "Tingo"
Ingolfsen, who reported "errors" on this disk). Both mount cleanly with `ndtool -t` but list
**zero files** — the directory structure is intact but empty, consistent with a bad capture
rather than a genuinely blank product floppy.

## Documentation
- No PD sheet, no PI sheet located.

## Provenance
Two real floppy images downloaded via NDwiki; both confirmed empty in this session. Not usable as
an install source. Logged here so the article number is indexed and not re-attempted without a
better image.

---
**Parent:** [../README.md](../README.md) (Software catalog)
